<?php

function compare_ships_menu() {
    $items['compare_ships'] = array(
        'title' => 'Cipher',
        'page callback' => 'compare_ships',
        'access callback' => TRUE,
        'type' => MENU_NORMAL_ITEM,
    );
    return $items;
}

function compare_ships() {
  return "test";
}
